from setuptools import find_packages, setup

setup(
    name='aiimc',
    packages=find_packages(include=['aiimc']),
    version='0.1.0',
    description='package to simulate AI in memory computing',
    author='Nanda',
    install_requires=[],
    setup_requires=[],
    tests_require=[],
    test_suite='',
)